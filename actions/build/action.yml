name: 'Hello World'
inputs:
  env-type: 
    required: true
    default: None
  hw-type:
    required: true
    default: None

outputs:
  random-number:
    value: 13
runs:
  using: "composite"
  steps:
    - run: echo build.${{ inputs.env-type }}.${{ inputs.hw-type }}
      shell: bash