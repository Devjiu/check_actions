name: 'Hello World'
description: 'Greet someone'
inputs:
  env-type: 
    required: true
    default: None
  hw-type:
    required: true
    default: None
  test-type:
    required: true
    default: None

outputs:
  random-number:
    description: "Random number"
    value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"
  steps:
    - run: echo test ${{ inputs.env-type }}.${{ inputs.hw-type }}.${{ inputs.test-type }}
      shell: bash